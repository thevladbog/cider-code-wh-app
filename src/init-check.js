// Проверка доступности React перед загрузкой приложения
window.addEventListener('DOMContentLoaded', () => {
  console.log('DOM loaded in index.html');
  console.log('Root element:', document.getElementById('root'));
});
